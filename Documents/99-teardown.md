# 講義が終了したら

## 完了時点のコードを Push

fork した github に、commit をして push をお願いします。  
master 以外のブランチで作業した場合は、そのブランチに push してください。

```shell
$ git commit -am "message"
$ git push origin master
```

## Firebase

Firebase Project の管理者から講師とTAを削除してください。  
Firebase Project 自体は消しても消さないままでも構いません。
